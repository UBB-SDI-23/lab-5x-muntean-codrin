import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#393646', // set the default background color
    },
    primary: {
      main: '#4F4557',
    },
    secondary: {
      main: '#6D5D6E',
    },
  },
});

export default theme;
